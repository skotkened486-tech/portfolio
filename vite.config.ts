import { defineConfig } from "vite";

// Publicado em https://jhollyfer.github.io/portfolio/
// Se um dia usar domínio próprio, troque para "/".
export default defineConfig({
  base: "/portfolio/",
});
